# Pterodactyl Panel Update Script

This repository contains a simple update script for the Pterodactyl Panel.  
It automates the normal update steps so you can update your panel with a single command.

---

## One-line update command

If GitHub Pages is enabled, you can run the updater directly with:

```bash
bash <(curl -sSL https://isthisvishal.github.io/Pterodactyl-Panel-Update-Rebuild-/pterodactyl.update)
